<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android"
    android:shareInterpolator="false">
    <scale
        android:duration="5000"
        android:fromXScale="1"
        android:fromYScale="1"
        android:pivotX="50%"
        android:pivotY="100%"
        android:toXScale="1"
        android:toYScale="0.6"/>
    <!--
    1:一次动画效果消耗的时间
    2，3：动画开始的缩放，0.0为不显示，1.0为正常大小
    4，5：动画起始位置，相对于屏幕的百分比，两个都为50%表示动画从屏幕中间开始
    6,7:动画最终缩放的倍数，1.0为正常大小，大于1.0为放大，小于则缩小
    android:startOffset="0":动画多次执行的间隔时间
    -->
</set>